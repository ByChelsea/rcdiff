from setuptools import find_packages, setup

description = 'RCDiff'

setup(
    name='rcdiff',
    version='1.0',
    python_requires='>=3.8',
    description=description,
    packages=find_packages(),
)
